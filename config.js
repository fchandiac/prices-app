const config = {
  serverUrl: "http://localhost:3001/", // Cambia esto a la URL de tu servidor
  // Puedes agregar más configuraciones aquí
  apiKey: "your-api-key", // Ejemplo de otra configuración
  timeout: 5000, // Ejemplo de tiempo de espera
};

export default config;
